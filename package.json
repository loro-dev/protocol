{
  "name": "loro-protocol-workspace",
  "private": true,
  "description": "pnpm workspace for Loro protocol packages",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "oxlint --type-aware",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@rslint/core": "^0.1.11",
    "oxlint": "^1.12.0",
    "oxlint-tsgolint": "^0.0.4",
    "prettier": "^3.6.2",
    "vitest": "^3.2.4"
  }
}
